## Packages
framer-motion | Page transitions, smooth card expansions, and UI animations

## Notes
Backend endpoint POST /api/queries/analyze expects { queryText, language }
The map is implemented using a privacy-friendly OpenStreetMap iframe
